# 名称

<!-- 
填写子智能体的唯一标识名称。
建议使用简洁、有意义的英文名称，如 "code-reviewer"、"test-generator" 等。
-->

{name}

# 描述

<!-- 
简要描述该子智能体的功能和用途。
说明它能够帮助用户完成哪些任务，以及它的核心能力是什么。
-->

{description}

# 场景提示词

<!-- 
设置子智能体的系统提示词/角色设定。
这些预设指令将帮助智能体更准确地理解开发需求，并以设定的方式协助完成开发任务。
可以包括：
- 角色定义（如"你是一个专业的代码审查专家"）
- 行为准则（如"始终遵循最佳实践"）
- 输出格式要求
- 特定领域知识说明
-->

{prompt}

# 工具

<!-- 
列出该子智能体可以使用的工具名称。
每行一个工具名称，例如：
- read_file
- edit_file
- terminal

注意：由于智能体不支持导入，这里只需填写工具名称即可。
如果不需要特定工具，可以留空。
-->

{tools}

# MCP

<!-- 
列出该子智能体需要使用的 MCP Server 名称。
每行一个 MCP Server 名称，例如：
- github-mcp
- database-mcp

注意：由于智能体不支持导入，这里只需填写 MCP Server 名称即可。
如果不需要 MCP，可以留空。
-->

{mcp}

# 知识库

<!-- 
列出该子智能体需要引用的知识库名称。
每行一个知识库名称，例如：
- project-docs
- api-reference

注意：由于智能体不支持导入，这里只需填写知识库名称即可。
如果不需要知识库，可以留空。
-->

{knowledge_base}
